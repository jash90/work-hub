#!/bin/bash
# Start the work-hub server in the foreground. launchd keeps it alive; see
# ~/Library/LaunchAgents/work-hub.plist
set -uo pipefail

cd "$(dirname "$0")/.." || exit 1
exec /usr/bin/python3 -m workhub.server "$@"
